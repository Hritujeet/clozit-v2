import StripeChekout from "@/components/StripeChekout";

const Page = () => {
    return (
        <main suppressHydrationWarning>
            <StripeChekout />
        </main>
    );
};

export default Page;
